╔════════════════════════════════════════════════════════════════╗
║            🚀 Git Push Guide - DNN Sentiment Project          ║
╔════════════════════════════════════════════════════════════════╗

📊 CURRENT FOLDER SIZE: 3.5GB
📦 SIZE AFTER GITIGNORE: ~50MB (safe for GitHub!)

═══════════════════════════════════════════════════════════════

STEP 1: Initialize Git Repository
═══════════════════════════════════════════════════════════════
cd "/Users/thinguyen/Library/CloudStorage/OneDrive-GrandCanyonUniversity/RECENT CLASSES/SHARED CLASSES/CST-435 JT/DNN"

git init
git branch -M main

═══════════════════════════════════════════════════════════════

STEP 2: Add Files (Excludes Large Files Automatically)
═══════════════════════════════════════════════════════════════
git add .

# Check what will be committed (should be ~50MB)
git status

═══════════════════════════════════════════════════════════════

STEP 3: Commit
═══════════════════════════════════════════════════════════════
git commit -m "Initial commit: DNN sentiment analysis project

- RNN + DistilBERT sentiment models
- Batch upload system with flexible CSV
- Analytics dashboard with SQLite
- Next.js frontend with 4 pages
- Training scripts and model comparison"

═══════════════════════════════════════════════════════════════

STEP 4: Create GitHub Repository
═══════════════════════════════════════════════════════════════
1. Go to: https://github.com/new
2. Repository name: dnn-sentiment-analysis
3. Description: Multi-model sentiment analysis with RNN & Transformer
4. Keep it PUBLIC or PRIVATE
5. DO NOT initialize with README (we have one)
6. Click "Create repository"

═══════════════════════════════════════════════════════════════

STEP 5: Push to GitHub
═══════════════════════════════════════════════════════════════
# Replace YOUR_USERNAME with your GitHub username
git remote add origin https://github.com/YOUR_USERNAME/dnn-sentiment-analysis.git

git push -u origin main

═══════════════════════════════════════════════════════════════

✅ WHAT GETS PUSHED (~50MB):
═══════════════════════════════════════════════════════════════
✓ Source code (api/, web/, model_training/)
✓ README.md and documentation
✓ RNN model (6MB)
✓ Transformer config files (tokenizer, vocab)
✓ Training results (metrics, figures)
✓ requirements.txt

❌ WHAT GETS EXCLUDED (~3.4GB):
═══════════════════════════════════════════════════════════════
✗ venv/ (1GB)
✗ node_modules/ (200MB)
✗ Transformer model files (1.7GB)
✗ Training checkpoints (1GB)
✗ Raw datasets (600MB)
✗ Database files (can regenerate)

═══════════════════════════════════════════════════════════════

🔥 TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════
If push fails with "file too large":

git rm --cached -r outputs/transformer
git commit --amend -m "Fix: Remove large transformer files"
git push -u origin main

═══════════════════════════════════════════════════════════════

📝 AFTER PUSH - Share This with Teammates:
═══════════════════════════════════════════════════════════════
"Large model files not included. To run:
1. Clone repo
2. Run: pip install -r requirements.txt
3. Train models: cd model_training && python model_b/train.py"

═══════════════════════════════════════════════════════════════
